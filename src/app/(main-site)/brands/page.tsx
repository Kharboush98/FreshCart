import { getUsertoken } from '@/lib/auth'
import React from 'react'

export default async function Brands() {

  //getting back the token of the user to use if neccessary
  // const myToken = await getUsertoken();
  // console.log(myToken);

  return (
    <div>
      Brand
    </div>
  )
}
